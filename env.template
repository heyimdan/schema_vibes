# Schema Validator - Production Environment Template
# =================================================
# Copy this file to .env.production and fill in your encrypted values

# CRITICAL: Master key for decrypting API keys
# Store this securely - never commit to version control!
SCHEMA_VALIDATOR_MASTER_KEY=your_master_key_here

# Encrypted API Keys (generated by encrypt_api_key.py)
# These are safe to store in configuration files
OPENAI_API_KEY=encrypted:your_encrypted_api_key_here

# Encrypted Admin Password (generated by encrypt_api_key.py)
# This is safe to store in configuration files
ADMIN_PASSWORD=encrypted:your_encrypted_admin_password_here

# Optional: Encrypted Anthropic API key
# ANTHROPIC_API_KEY=encrypted:your_encrypted_anthropic_key_here

# Application Configuration
AI_PROVIDER=openai
GPT_MODEL=gpt-4o-mini
DEBUG=false
API_HOST=0.0.0.0
API_PORT=8000

# Database Configuration
CHROMA_PERSIST_DIRECTORY=./chroma_db
DATABASE_URL=sqlite:///./schema_validator.db

# Logging
LOG_LEVEL=INFO

# Optional: Redis Configuration
# REDIS_URL=redis://localhost:6379/0
# CACHE_TTL=3600

# Instructions:
# 1. Run `python encrypt_api_key.py` to generate encrypted keys
# 2. Copy the master key and encrypted API key from the script output
# 3. Set SCHEMA_VALIDATOR_MASTER_KEY to the master key
# 4. Set OPENAI_API_KEY to the encrypted API key (including "encrypted:" prefix)
# 5. Save this file as .env.production (it will be ignored by git) 